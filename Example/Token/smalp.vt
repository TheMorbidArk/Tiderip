import people for People
function fn() {
   tide p = People.new("xiaoming", "male")
   p.sayHi()
}

class Family < People {
   tide father
   tide mother
   tide child
   new(f, m, c) {
      father = f
      mother = m
      child  = c
      super("wbf", "male")
   }
}

tide f = Family.new("wbf", "ls", "shine")
f.sayHi()

fn()
